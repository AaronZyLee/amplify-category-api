version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 14
  pre_build:
    commands:
      - yarn production-build
      - yarn build-tests
      - echo 'Pre-Build phase completed!'
  build:
    commands: 
      - echo 'Build phase completed!'
artifacts:
  files:
    - '**/*'